{
  "name": "decision-log-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/test/*.js",
    "check": "biome check src/",
    "format": "biome check --write src/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.15",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
